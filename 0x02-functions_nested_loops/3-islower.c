#include "main.h"

/**
 * _islower - checks for lowercase letters or characters
 * @lower is letter for checking
 * Return: 1 if a letter is a lowercase otherwise 0
 */

int _islower(int lower)
{
return (lower >= 'a' && lower <= 'z');
}
